package com.example.focullpointv2.ui.culling

/** Intent-extra keys used to launch [CullingActivity] from the setup screen. */
object CullingExtras {
    const val SOURCE_PATH = "extra_source_path"
    const val FAVORITE_PATH = "extra_favorite_path"
    const val REJECT_PATH = "extra_reject_path"
}
